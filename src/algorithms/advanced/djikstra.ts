import { Node } from "@/components/advancedAlgos/Djikstra";

export const wrappedDjikstra = (graph: Node[]) => {
  // copy graph
  let copyGraph: Node[] = graph;
  // TODO: change this type
  let steps: any[] = [];

  const djikstra = (graph: Node[]) => {};

  djikstra(copyGraph);

  return steps;
};
